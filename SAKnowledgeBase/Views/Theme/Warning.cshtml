@using SAKnowledgeBase.DataBase.Entities;

@model Theme

<div class="row">
    <div class="col-lg-4 offset-lg-4 col-md-6 offset-md-3 col-12">
        <div class="card">
            <div class="card-header">
                <div class="alert alert-danger">
                    <h5>You can't delete</h5>
                </div>
                Category ID @Model.Id
            </div>
            <div class="card-body">
                <p class="card-text">Category Name: @Model.ThemeName</p>
                <div class="alert alert-danger">
                    <h5>To delete this category, you need to edit contacts.</h5>
                </div>
                @if (@Model.Questions.Count != 0)
                {
                    <label>Contacts List:</label>
                    <table class="table">
                        <thead>
                            <tr>
                                <td>QuestionName</td>
                                <td>Info</td>
                                <td>Manage</td>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Questions)
                            {
                                <tr>
                                    <td>@item.QuestionName</td>
                                    <td>
                                        <a asp-action="Details" asp-controller="Questions" class="btn btn-primary" asp-route-id="@item.Id">
                                            View
                                        </a>
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-controller="Questions" class="btn btn-warning" asp-route-id="@item.Id">
                                            Edit
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p class="card-text">No question in theme.</p>
                }
                <a class="btn btn-secondary" asp-controller="Theme" asp-action="Index">Cancel</a>
            </div>

        </div>
    </div>
</div>
